class DayChecker {

    getDays(num) {
        let days = ['Saturday', 'Sunday', 'Monday',
            'Tuseday', 'Wednesday', 'Thursday', 'Friday'];

        return days[num];
    }
};

export default DayChecker;
